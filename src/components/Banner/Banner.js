import React from 'react';
import './Banner.css';

const Banner = () => (
  <div className="banner text-center d-flex align-items-center justify-content-center">
    <p className="m-0">Click on a picture, but don't click it twice!</p>
  </div>
);

export default Banner;
